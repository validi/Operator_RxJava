<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aurora.Operator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.From">
        <activity android:name=".SkipOperator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DistinctOperator">

        </activity>
        <activity android:name=".FilterOperator"></activity>
        <activity android:name=".BufferOperator" />
        <activity android:name=".ConcatMapOperator" />
        <activity android:name=".FlatMapOperator" />
        <activity android:name=".MapOperator" />
        <activity android:name=".CreateOperator" />
        <activity android:name=".RangeOperator" />
        <activity android:name=".FromArray" />
    </application>

</manifest>